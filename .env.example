# Database configuration for local development (used when NOT running via Docker Compose)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=gestion_du_soutien
DB_USER=root
DB_PASS=your_mysql_password
DB_USE_SSL=false
